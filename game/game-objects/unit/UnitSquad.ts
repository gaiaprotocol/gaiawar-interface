import { Fadeable } from "@gaiaengine/2d";
import TileFaction from "../../data/tile/TileFaction.js";

export default class UnitSquad extends Fadeable {
  constructor(faction: TileFaction, unitId: number, quantity: number) {
    super(0, 0);

    //TODO:
  }

  public setQuantity(quantity: number) {
    //TODO:
  }
}
